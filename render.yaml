services:
  - type: web
    name: store-media-webapp
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: GEMINI_API_KEY
        sync: false        # Render 대시보드에서 직접 입력
